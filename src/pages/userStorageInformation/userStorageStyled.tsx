import styled from 'styled-components';
import { theme } from '../../style/theme';

export const DivUserStorage = styled.div`
  background-color: ${theme.background};
  display: flex;
  flex-direction: column;
  width: 25%;
  box-sizing: border-box;
`;
